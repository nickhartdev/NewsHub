import React from 'react';
import { link } from 'react-router-dom';

const Form = (props) => {
  return (
    <section className="form">
      <h2>Form</h2>
    </section>
  );
  
}

export default Form;